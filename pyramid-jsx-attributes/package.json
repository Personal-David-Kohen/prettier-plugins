{
  "name": "prettier-plugin-pyramid-jsx-attributes",
  "version": "1.0.0",
  "description": "Prettier plugin that orders JSX attributes into a shortest-to-longest pyramid",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "keywords": [
    "prettier",
    "prettier-plugin",
    "jsx",
    "tsx",
    "attributes",
    "pyramid"
  ],
  "license": "MIT",
  "peerDependencies": {
    "prettier": ">=3.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "prettier": "^3.0.0",
    "typescript": "^6.0.2"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node test/test.js && node test/integration.js",
    "prepublishOnly": "npm run build"
  }
}
